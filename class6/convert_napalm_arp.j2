---
{% for arp_entry in napalm_arp_table %}
{{ arp_entry['ip'] }}: {{ arp_entry['mac'] }}
{% endfor %}
# This jinja2 template will generate a YAML file, which includes IP: Mac pairs (which is a YAML dictionary)
